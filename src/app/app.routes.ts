import { Routes } from '@angular/router';
import { RepoAnalyzerComponent } from './components/repo-analyzer/repo-analyzer.component';

export const routes: Routes = [
    {path : '', component: RepoAnalyzerComponent}
];
